//I wrote this without using eclipse, so this might not work at first... Until I can debug it, and add the commands

com.ekclifford.Main.Main

import org.bukkit.plugin.java.JavaPlugin;
import org.bukkit.Bukkit;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;

public class Main extends JavaPlugin{

@Override
public onEnable(){
getLogger().info("Plugin Enabled!");
this.saveDefaultConfig();
List<EnglishWords> = this.getConfig().getStringList("EnglishWords");
List<DutchWords> = this.getCOnfig().toStringList("DutchWords");
}
public onDisable(){
getLogger().info("Plugin Disabled!");
}

public boolean oncommand(CommandSender sender, Command cmd, String label, String[] args){
if(cmd.getName().equalsIgnoreCase("english")){
Player player = (Player) sender;
if(player.hasPermission("translate.use") || (player.isOp())){


}

}
else if(cmd.getName().equalsIgnoreCase("dutch")){
Player player = (Player) sender;
if(player.hasPermission("translate.use") || (player.isOp())){


}


}
}
